# Mentor UI (달력 제외 + 네비/기능)

## 실행
```bash
npm install
npm run dev
```
브라우저: http://localhost:5173

## 구현된 기능
- 홈 상단 '알림' 버튼 → 알림 화면 이동
- 홈 '과제' / '할 일' 섹션 → 과제/할 일 화면(탭 선택 상태로) 이동
- 과제/할 일 화면
  - 탭 전환(과제/할 일/과목별 피드백)
  - 완료 체크 토글
  - 할 일 탭에서 '할 일 추가' FAB → 모달에서 추가 (로컬 state 반영)
